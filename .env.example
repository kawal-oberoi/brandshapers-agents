# Copy this file to ".env" and replace the placeholder values with your real
# tokens. Do NOT commit the real .env file — it is ignored by git.

# Slack "Bot User OAuth Token" — starts with xoxb-
SLACK_BOT_TOKEN=xoxb-your-bot-token-here

# Slack "App-Level Token" with the connections:write scope — starts with xapp-
SLACK_APP_TOKEN=xapp-your-app-level-token-here

# Anthropic API key — starts with sk-ant-
ANTHROPIC_API_KEY=sk-ant-your-api-key-here

# Apollo.io API key — found in Apollo under Settings → Integrations → API.
# Used by Agent 2 (the Sourcer) for People Search (free) and Enrichment (~1 credit).
APOLLO_API_KEY=your-apollo-api-key-here

# --- Agent 2 (Sourcer) settings — all optional, sensible defaults shown ---
# Most enrichment credits Agent 2 may spend per calendar month (protects credits).
APOLLO_MONTHLY_CAP=70
# Slack channel where sourcing summaries are posted.
OUTREACH_CHANNEL=#outreach-control
# Weekly auto-sourcing schedule (set ENABLED to false to turn it off).
SOURCER_SCHEDULE_ENABLED=true
SOURCER_SCHEDULE_DAY=mon
SOURCER_SCHEDULE_HOUR=9
SOURCER_SCHEDULE_MINUTE=0
SOURCER_SCHEDULE_TZ=Asia/Kolkata
SOURCER_SCHEDULE_MAX_ENRICH=10

# --- Agent 6 (Intent Scout) settings — all optional, sensible defaults shown ---
# Apify API token — apify.com → Settings → Integrations → API. Used to scrape
# LinkedIn posts (the actor harvestapi/linkedin-post-search). No LinkedIn login.
APIFY_API_TOKEN=apify_api_your-token-here
# Hard monthly Apify budget in USD (the $5 free plan — never exceed this).
APIFY_MONTHLY_BUDGET_USD=4.50
# Estimated Apify cost per scraped post (used for the budget guard).
APIFY_COST_PER_POST=0.002
# Most posts to scrape per weekly run (split evenly across active queries).
SCOUT_MAX_POSTS=125
# Only scrape posts from the last N days.
SCOUT_POSTED_DAYS=14
# Weekly auto-scout schedule (set ENABLED to false to turn it off).
SCOUT_SCHEDULE_ENABLED=true
SCOUT_SCHEDULE_DAY=tue
SCOUT_SCHEDULE_HOUR=9
SCOUT_SCHEDULE_MINUTE=0
SCOUT_SCHEDULE_TZ=Asia/Kolkata

# The ID of the Google Sheet used as Agent 1's source of truth (Segments,
# Briefs, Leads, Meta). (It's the long string in the sheet's URL between /d/
# and /edit.)
GOOGLE_SHEET_ID=your-google-sheet-id-here

# The ID of the SEPARATE Google Sheet Agent 6 uses for campaigns. It holds two
# tabs: CampaignBriefs (your live campaigns, added via "add campaign" in Slack)
# and PublisherLeads (the scraped posts + drafted comments). Share this sheet
# with the SAME service-account email as the main sheet. Leave blank to disable
# Agent 6's campaign hunting.
CAMPAIGN_SHEET_ID=your-campaign-sheet-id-here

# Google service account credentials.
#  - Locally: leave this blank and keep the google-credentials.json file in the
#    project folder. The bot reads the file automatically.
#  - On Railway: paste the ENTIRE contents of google-credentials.json as the
#    value of this variable. The bot prefers this env var when it is set.
GOOGLE_CREDENTIALS_JSON=
